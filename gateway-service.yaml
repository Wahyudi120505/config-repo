spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: user-service
          url: lb://user-service
          predicates:
            - path=/**
        
        - id: product-service
          url: lb://product-service
          predicates:
            - path=/**

        - id: cart-service
          url: lb://cart-service
          predicates:
            - path=/**